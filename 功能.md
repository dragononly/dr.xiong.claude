新增功能
1.可以修改 claude 的 key

2.可以查询订阅信息（额度上限）
# 查询订阅信息（额度上限）
curl -X GET "http://serve2.moono.vip:3011/v1/dashboard/billing/subscription" \
  -H "Authorization: Bearer sk-你的key"

# 查询使用量
curl -X GET "http://serve2.moono.vip:3011/v1/dashboard/billing/usage?start_date=2024-01-01&end_date=2026-12-31" \
  -H "Authorization: Bearer sk-你的key