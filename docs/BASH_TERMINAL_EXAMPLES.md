# BashTerminalTool 使用示例

## 快速测试

重新加载 VSCode 扩展后，可以尝试以下对话：

### 测试 1：简单命令
```
用户：在终端中执行 echo "Hello Terminal"

AI：我来在 VSCode 终端中执行这个命令。

bash_terminal(command="echo 'Hello Terminal'", terminalName="Test")

命令已在 VSCode 终端 "Test" 中执行。请查看终端面板查看输出。
```

### 测试 2：查看文件
```
用户：帮我用终端查看 src 目录的文件

AI：我会在终端中执行 ls 命令查看文件。

bash_terminal(command="ls -la src", terminalName="File Browser", description="列出 src 目录文件")

命令已在终端中执行，请查看终端面板。
```

### 测试 3：可能的卡住场景
```
用户：安装一个可能会有问题的 npm 包

AI：我会在终端中执行安装命令，这样如果卡住你可以手动 Ctrl+C。

bash_terminal(
    command="npm install",
    terminalName="NPM Install",
    description="安装 npm 依赖"
)

安装命令已在终端中执行。
- 你可以看到实时输出
- 如果卡住可以按 Ctrl+C
- 终端会保持打开状态
```

### 测试 4：调试构建
```
用户：帮我构建项目，但我想看到详细的输出

AI：好的，我会在终端中执行构建命令，这样你可以看到完整的构建过程。

bash_terminal(
    command="npm run build",
    terminalName="Build",
    description="构建项目"
)

构建命令已在终端 "Build" 中执行。
- 查看终端面板了解构建进度
- 如果有错误可以看到详细堆栈
- 可以手动调整代码后重新构建
```

## 实际使用场景

### 场景 1：安装依赖时遇到问题

**之前的问题**：
```
AI: bash_exec(command="npm install webpack", timeout=120000)
[卡住了，无法干预，只能等待超时]
```

**现在的解决方案**：
```
AI: bash_terminal(command="npm install webpack", terminalName="NPM Install")
[在终端中执行，可以看到进度]
[如果卡住，用户可以手动 Ctrl+C]
[可以继续在终端输入其他命令]
```

### 场景 2：调试复杂的构建脚本

**之前**：
```
AI: bash_exec(command="npm run build")
[只能看到最终输出，无法调试]
```

**现在**：
```
AI: bash_terminal(command="npm run build", terminalName="Build Debug")
[可以看到每一步的输出]
[更容易定位问题]
```

### 场景 3：启动开发服务器

**之前**：
```
AI: bash_exec(command="npm run dev", run_in_background=true)
[返回 bash_id，需要用 bash_output 查看输出]
[不方便实时查看]
```

**现在**：
```
AI: bash_terminal(command="npm run dev", terminalName="Dev Server")
[在终端中看到实时日志]
[可以随时停止服务器]
```

## AI 智能选择

AI 会根据情况自动选择合适的工具：

### 自动使用 bash_exec
- 简单命令：ls, cat, pwd
- Git 命令：git status, git log
- 快速操作：npm run test, npm run lint

### 自动使用 bash_terminal
- 安装依赖：npm install
- 构建项目：npm run build（如果用户提到"调试"或"详细输出"）
- 启动服务器：npm run dev
- 用户提到"可能卡住"、"需要查看"、"手动干预"

### 用户可以明确指定

你也可以明确告诉 AI 使用哪个工具：

```
用户：用终端执行这个命令，我想看到输出
AI: 好的，我使用 bash_terminal...

用户：后台执行这个命令
AI: 好的，我使用 bash_exec...
```

## 技巧

1. **给终端命名**：使用 `terminalName` 参数，方便区分多个终端
   ```typescript
   bash_terminal(command="npm run dev", terminalName="Frontend Dev")
   bash_terminal(command="npm run backend", terminalName="Backend Server")
   ```

2. **添加描述**：使用 `description` 参数，让 AI 记录命令目的
   ```typescript
   bash_terminal(
       command="npm install",
       description="安装项目依赖"
   )
   ```

3. **切换工作目录**：使用 `cwd` 参数
   ```typescript
   bash_terminal(
       command="pwd",
       cwd="/tmp"
   )
   ```

## 注意事项

1. **终端会保持打开**：命令执行完后终端不会关闭，方便继续操作
2. **多个终端**：每次调用会创建或复用同名终端
3. **手动清理**：如果终端太多，可以手动关闭不需要的终端
4. **输出不捕获**：bash_terminal 不会自动捕获输出，需要手动查看终端

## 下一步

- 尝试在不同场景使用 bash_terminal
- 告诉 AI 你的使用体验
- 提出改进建议

享受更好的终端体验！🎉
